#pragma once

int add(int i, int j);

int multiply(int i, int j);

int shift(int x, int shift);

class my_obj {
    public: 
    int x;
    int y;

    my_obj(int x, int y);

    int func();
};
